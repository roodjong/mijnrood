{% use "form_div_layout.html.twig" %}

{%- block form_errors -%}
    {% if errors|length > 0 %}
    <div class="message message-error">
        {{ parent() }}
    </div>
    {% endif %}
{%- endblock form_errors -%}
